let isBeginner: boolean = true; //boolean
let total: number = 10; //number
let firstName: string = "james"; //string
let sentence: string = `hello, I am ${firstName}`; //string
let u: undefined = undefined; //undefined
let n: null = null; //null
let list1: number[] = [1, 2, 3, 4]; //arrays
let list2: Array<number> = [1, 2, 3, 4]; //arrays
let person1: [string, number] = ["James", 21]; //tuple
enum Color {
  red,
  green,
  blue,
} //enum
let c: Color = Color.green;
console.log(c);
let randomValue: any = 10;
