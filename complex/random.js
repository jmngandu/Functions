// Random generates random numbers between  0 and 1
// Floor in the other case is used for

let randomNum = Math.random() * 9; // it will generate random numbers between 0 and 1
console.log(randomNum);

let floorNum = Math.floor(5.999); // it will removes decimal
console.log(floorNum);
let randNum = Math.floor(Math.random() * 20); // it generates random numbers between 1 to 19 without decimal places

console.log(randNum);
